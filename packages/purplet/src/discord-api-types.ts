// Re-export `discord-api-types` to make it available without installing it, as well as sync version
// updates so the dev doesnt have to ctrl+f all uses of 'v10' and make it 'v11'
export * from 'discord-api-types/v10';
